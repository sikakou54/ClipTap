<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="360dp"
    android:minHeight="360dp"
    android:maxHeight="360dp">

    <!-- メインビュー（リスト表示） -->
<LinearLayout
    android:id="@+id/mainView"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="?android:attr/colorBackground"
    android:padding="8dp">

    <!-- 統合フィルターエリア（環境ドロップダウン + カテゴリスクロール） -->
    <LinearLayout
        android:id="@+id/filterContainer"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:orientation="horizontal"
        android:background="@android:color/transparent">

        <!-- 環境選択ドロップダウンボタン（左端固定、固定幅100dp） -->
        <com.google.android.material.chip.ChipGroup
            android:id="@+id/profileChipGroup"
            android:layout_width="100dp"
            android:layout_height="match_parent"
            app:singleSelection="true"
            app:chipSpacing="0dp" />

        <!-- カテゴリ選択チップグループ（残りのスペースを使用） -->
        <HorizontalScrollView
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:scrollbars="none">

            <com.google.android.material.chip.ChipGroup
                android:id="@+id/categoryChipGroup"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                app:singleSelection="true"
                app:chipSpacing="8dp" />

        </HorizontalScrollView>

    </LinearLayout>

    <!-- スニペット一覧とEmptyビューのコンテナ（固定高さ：親の高さ360dp - padding 16dp - filter 44dp - marginTop 8dp = 292dp） -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="292dp"
        android:layout_marginTop="8dp">

        <!-- スニペット一覧 -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/snippetRecyclerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="?android:attr/colorBackground"
            android:clipToPadding="false"
            android:scrollbars="vertical" />

        <!-- 空の状態表示 -->
        <TextView
            android:id="@+id/emptyStateTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="@string/snippet_empty"
            android:textSize="16sp"
            android:textColor="#999999"
            android:visibility="gone" />

    </FrameLayout>

</LinearLayout>

    <!-- 詳細ビュー（全画面表示、初期状態は非表示） -->
    <include layout="@layout/snippet_detail_view" />

</FrameLayout>

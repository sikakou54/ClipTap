# 開発モード（npm run dev:web）で読み込まれる設定
# ClipTap API のベースURL。既定はローカルの wrangler dev（npm run dev:api）
VITE_API_BASE_URL=http://localhost:8787

# デプロイ済みの開発用Worker（cliptap-api-dev）へ向けたい場合は
# .env.development.local を作成し、次の値で上書きする（gitignore済み）
#   VITE_API_BASE_URL=https://cliptap-api-dev.sikakou.workers.dev
